name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - name: Inject version
        run: |
          VERSION=$(TZ='America/New_York' git log -1 --format='%cd' --date=format:'%Y-%m-%d %H:%M')
          sed -i "s/__VERSION__/v$VERSION/" index.html
          sed -i "s/__CACHE_VERSION__/${{ github.sha }}/" sw.js
      - uses: actions/configure-pages@v4
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - id: deployment
        uses: actions/deploy-pages@v4
